name: Deploy snowpark app

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python 3.9
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        working-directory: snowpark_project
        run: |
          pip install -r requirements.txt
          pip install snowflake-cli-labs

      - name: Deploy Snowpark apps
        working-directory: snowpark_project
        env:
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_ROLE: ${{ secrets.SNOWFLAKE_ROLE }}
          SNOWFLAKE_WAREHOUSE: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
          SNOWFLAKE_DATABASE: ${{ secrets.SNOWFLAKE_DATABASE }}
        run: |
          snow sql -q "USE DATABASE $SNOWFLAKE_DATABASE;" --temporary-connection
          snow sql -q "USE SCHEMA $SNOWFLAKE_SCHEMA;" --temporary-connection
      
          snow sql -q "DROP PROCEDURE IF EXISTS $SNOWFLAKE_DATABASE.$SNOWFLAKE_SCHEMA.hello_procedure(string);" --temporary-connection
          snow sql -q "DROP PROCEDURE IF EXISTS $SNOWFLAKE_DATABASE.$SNOWFLAKE_SCHEMA.test_procedure();" --temporary-connection
          snow sql -q "DROP FUNCTION IF EXISTS $SNOWFLAKE_DATABASE.$SNOWFLAKE_SCHEMA.hello_function(string);" --temporary-connection
          snow snowpark build --allow-shared-libraries
          snow snowpark deploy --replace --temporary-connection
